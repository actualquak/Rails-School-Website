<%= bootstrap_form_for(@comment) do |f| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h5><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h5>
      <ul>
        <% @comment.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.hidden_field :article_id, value: 1 %>
  <%= f.hidden_field :parent_id %>
  <div class="form-group">
    <%= f.text_area :content, class: 'form-control', required: true, hide_label: true %>
  </div>

  <%= f.submit class: 'btn btn-primary' %>
<% end %>
